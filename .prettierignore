# Prettier already respects .gitignore. This file is for things that are tracked
# but must not be reformatted.

# Generated by `pnpm idl:sync` from the Anchor build output. Formatting it would
# make `pnpm idl:check` compare prettier's output against the generator's and
# fail forever — the check exists to catch a stale IDL, not a stale style.
packages/escrow/src/idl.ts
packages/escrow/src/types.ts

# Generated by `pnpm corpus:build` from `computeStandings`. Same reasoning as the
# IDL above: `corpus.test.ts` compares the checked-in bytes against what the
# generator produces, so letting prettier reformat it would make that comparison
# fail forever — and the comparison is what stops the Rust derivation kernel
# drifting from the TypeScript that decides the same question.
packages/core/src/season/conformance/standings-corpus.json
