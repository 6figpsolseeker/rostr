# Prettier already respects .gitignore. This file is for things that are tracked
# but must not be reformatted.

# Generated by `pnpm idl:sync` from the Anchor build output. Formatting it would
# make `pnpm idl:check` compare prettier's output against the generator's and
# fail forever — the check exists to catch a stale IDL, not a stale style.
packages/escrow/src/idl.ts
